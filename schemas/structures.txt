# Users
Users:
    id: u32
    username: String,
    password: String

# Currenies associated with users
Currency:  
    id: u32
    name: String
    user_id: u32  # Relation to user
    balance: u32

# Item within an inventory
InventoryItem:
    id: UUID
    user_id: u32  # Relation to user
    definition_name: UUID
    stack_size: u32
    seen: bool
    instance_attributes: json map of string to value
    created: Date
    last_grant: Date
    earned_by: String
    restricted: bool

SeenArticles:
    id: u32
    user_id: u32  # Relation to user
    article_id: UUID

Character:
    id: UUID
    user_id: u32  # Relation to user

    class_name: UUID # Relation to class data
    name: UUID
    level: u32
    xp: JSON

ClassData:
    id: u32
    user_id: u32
    name: UUID # 5762714e-ef94-4651-b6b5-4d16cdb0d0b1
    unlocked: bool